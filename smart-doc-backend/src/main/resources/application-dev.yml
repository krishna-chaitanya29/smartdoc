spring:
  datasource:
    url: jdbc:mysql://localhost:3306/smartdoc_dev
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

logging:
  level:
    root: INFO
    '[com.example.smartdoc]': DEBUG
